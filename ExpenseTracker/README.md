# Expense Tracker (ASP.NET Core MVC + SQLite)

Requirements: .NET 8 SDK

    dotnet restore
    dotnet run

Open the https/http URL shown in the console. The database file (expenses.db) is created automatically.
